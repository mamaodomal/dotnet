dentro da pasta server
  docker build -t server .

docker run -it --rm -p 3000:80/tcp --name servercontainer server

dentro da pasta angular
  docker build -t frontend .

docker run -d -it -p 4000:80/tcp --name angular frontend
